<h1>Restaurants#index</h1>
<p>Find me in app/views/restaurants/index.html.erb</p>
<ul>
<%= link_to 'new', new_restaurant_path %>
    <% @restaurants.each do |item| %>
     <li> <%= item.name%> <%= link_to 'show', restaurant_path(item) %> </li>
    <%end%>
</ul>